# Widget API

> **ตัดสินใจแล้ว (2026-07-16):** ไฟล์นี้ไม่ใช่สเปกคู่ขนานอีกไฟล์ — contract ฉบับเต็มของ Widget
> API อยู่ที่ `development/docs/WIDGET_API.md` เท่านั้น (180 บรรทัด ครอบ folder structure,
> metadata.json, lifecycle, events, settings ครบ) เอกสารนี้เก็บไว้เป็นแค่ตัวชี้ทาง เพื่อกัน
> ปัญหาสองไฟล์ที่พูดเรื่องเดียวกัน drift ไปคนละทางเมื่อมีคนแก้ไฟล์ใดไฟล์หนึ่งแล้วลืมอีกไฟล์
>
> ถ้า task 12-14 (Widget Edit Mode / Drag & Drop / Grid Engine) ต้องเพิ่ม API ใหม่ให้ widget
> เรียก (เช่น hook สำหรับ back-side actions ของ Edit Mode) ให้เพิ่มเข้าไปใน
> `development/docs/WIDGET_API.md` โดยตรง ไม่ต้องสร้าง section คู่ขนานที่นี่

Widget manifest, lifecycle, events — ดู `development/docs/WIDGET_API.md`
