Each subfolder is one theme pack: <id>/theme.json (+ optional screenshot.png).
See _template/theme.json for the manifest shape. Discovered by
lib/themePackRegistry.js — read by the Widget Center overlay's Themes tab,
see lib/widgetCenterOverlay.js.
